on: [push]

name: IoT_Dev

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

    - name: Login via Az module
      uses: azure/login@v1
      with:
        environment: Iot_Dev
        creds: ${{secrets.AZURE_CREDENTIALS}}
        enable-AzPSSession: true 
